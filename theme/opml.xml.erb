<opml version="1.1">
  <head>
    <title><%= site.title %></title>
    <dateModified><% site.fetched %></dateModified>
    <ownerName>Shaun McDonald</ownerName>
    <ownerEmail>osm@shaunmcdonald.me.uk</ownerEmail>
  </head>
  <body>
    <% site.feeds.each do |feed| %><outline type="rss" text="<%= feed.title %>" xmlUrl="<%= feed.url %>" title="<%= feed.title %>"/>
    <% end %>
  </body>
</opml>